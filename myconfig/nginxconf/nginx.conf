#user  nobody;
worker_processes  10;

#error_log  logs/error.log;
#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;
error_log logs/error.log; 
pid logs/nginx.pid;

events {
    worker_connections  1024;
}

http {
    access_log  logs/access.log;
    include       mime.types;
    default_type  application/octet-stream;


    log_format  main  '$remote_addr - $remote_user [$time_local] $request '
                      '"$status" $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  60;

    gzip on;
    gzip_min_length  1k;
    gzip_buffers     4 8k;
    gzip_http_version 1.1;
    gzip_types       text/plain application/x-javascript text/css application/xml;

    server {
        listen       80;
        server_name  localhost;
        autoindex on;
        autoindex_exact_size on;
        autoindex_localtime on;

        #charset;
        #charset  gb2312;

        #access_log  logs/host.access.log  main;


        location / {
            root   /home/liulyliu/code/;
            index  index.html index.htm index.php;
            autoindex on;
            autoindex_exact_size off;
            autoindex_localtime on;
        }


        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;

        #location = /50x.html {
        #    root   html;
        #}

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #

        location ~ \.php$ {
            fastcgi_pass   127.0.0.1:9000;
            fastcgi_index  index.php;
            if (  !-e $document_root$fastcgi_script_name ) {
                return 404;
            }

            fastcgi_param  SCRIPT_FILENAME  $document_root$fastcgi_script_name;
            include        fastcgi_params;
        }


        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }

    server {
        listen 80;
        server_name abc.tv.sohu.com;
        autoindex on;
        autoindex_exact_size on;
        autoindex_localtime on;

        location / {
            root   /Users/vesper/code/sohu/;
            index  index.html index.htm;
        }
    }

    ##tv
    server {
        listen          80;
        server_name     tv.sohu.com;
        proxy_redirect          off;
        proxy_set_header        Host tv.sohu.com;
        proxy_set_header        X-Real-IP $remote_addr;
        proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout   60;
        proxy_send_timeout      60;
        proxy_read_timeout      60;
        proxy_buffering         off;
        proxy_buffer_size       8k;
        proxy_buffers           8 32k;
        proxy_busy_buffers_size 64k;
        proxy_headers_hash_bucket_size  64;
        proxy_headers_hash_max_size     512;
        proxy_ignore_client_abort       on;
        proxy_temp_file_write_size      64k;
        proxy_max_temp_file_size        0;
        location  / {
            root /home/liulyliu/test/;
            if (!-f $request_filename) {
                proxy_pass      http://61.135.132.59;
                #proxy_pass      http://10.11.5.95;
                #proxy_pass      http://tv.sohu.com/$uri;
            }
            if (-f $request_filename) {
            }
        }
    }

    ##mytv
    server {
        listen          80;
        server_name     my.tv.sohu.com;
        proxy_redirect          off;
        proxy_set_header        Host my.tv.sohu.com;
        proxy_set_header        X-Real-IP $remote_addr;
        proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout   60;
        proxy_send_timeout      10;
        proxy_read_timeout      10;
        proxy_buffering         off;
        proxy_buffer_size       8k;
        proxy_buffers           8 32k;
        proxy_busy_buffers_size 64k;
        proxy_headers_hash_bucket_size  64;
        proxy_headers_hash_max_size     512;
        proxy_ignore_client_abort       on;
        proxy_temp_file_write_size      64k;
        proxy_max_temp_file_size        0;

        #location ~* /profile_update.do$ {
        ##location /profile/profile_update.do {
            #proxy_pass http://10.10.77.105:8080;
        #}
        location  / {
            #add_header Access-Control-Allow-Origin *;
            #setenv.add-response-header = ( "Access-Control-Allow-Origin" => "*" )
            #proxy_redirect          http://my.tv.sohu.com;
            root /Users/vesper/code/mytv/;
            if (!-f $request_filename) {
                #proxy_pass      http://10.11.132.62;
                #proxy_pass      http://10.11.132.86;
                proxy_pass      http://220.181.11.132;
            }
        }
    }

    server {
		listen          80;
		server_name     js1.my.tv.sohu.com.cn;
		proxy_redirect          off;
		proxy_set_header        Host js1.my.tv.sohu.com.cn;
		proxy_set_header        X-Real-IP $remote_addr;
		proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_connect_timeout   60;
		proxy_send_timeout      60;
		proxy_read_timeout      60;
		proxy_buffering         off;
		proxy_buffer_size       8k;
		proxy_buffers           8 32k;
		proxy_busy_buffers_size 64k;
		proxy_headers_hash_bucket_size  64;
		proxy_headers_hash_max_size     512;
		proxy_ignore_client_abort       on;
		proxy_temp_file_write_size      64k;
		proxy_max_temp_file_size        0;
		location  / {
            root /Users/vesper/code/mytv/;
			rewrite ^(/.*)styles.[0-9]+(.*)$         $1styles$2  last;
			rewrite ^(/.*)scripts.[0-9]+(.*)$        $1scripts$2 last;

			if (!-f $request_filename) {
				proxy_pass      http://220.181.26.7;
				#proxy_pass      http://10.11.5.95;
				#proxy_pass      http://tv.sohu.com/$uri;
			}
		}
	}
    server {
		listen          80;
		server_name     js2.my.tv.sohu.com.cn;
		proxy_redirect          off;
		proxy_set_header        Host js2.my.tv.sohu.com.cn;
		proxy_set_header        X-Real-IP $remote_addr;
		proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_connect_timeout   60;
		proxy_send_timeout      60;
		proxy_read_timeout      60;
		proxy_buffering         off;
		proxy_buffer_size       8k;
		proxy_buffers           8 32k;
		proxy_busy_buffers_size 64k;
		proxy_headers_hash_bucket_size  64;
		proxy_headers_hash_max_size     512;
		proxy_ignore_client_abort       on;
		proxy_temp_file_write_size      64k;
		proxy_max_temp_file_size        0;
		location  / {
            root /Users/vesper/code/mytv/;
			rewrite ^(/.*)styles.[0-9]+(.*)$         $1styles$2  last;
			rewrite ^(/.*)scripts.[0-9]+(.*)$        $1scripts$2 last;

			if (!-f $request_filename) {
				proxy_pass      http://220.181.26.7;
				#proxy_pass      http://10.11.5.95;
				#proxy_pass      http://tv.sohu.com/$uri;
			}
		}
	}



    #images.sohu.com
    server {
        listen          80;
        server_name     images.sohu.com;

        location / {
            return 204;
        }
    }


    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}


    # HTTPS server
    #
    #server {
    #    listen       443;
    #    server_name  localhost;

    #    ssl                  on;
    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_timeout  5m;

    #    ssl_protocols  SSLv2 SSLv3 TLSv1;
    #    ssl_ciphers  ALL:!ADH:!EXPORT56:RC4+RSA:+HIGH:+MEDIUM:+LOW:+SSLv2:+EXP;
    #    ssl_prefer_server_ciphers   on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}

    #zebra
    #css
    server {
        listen          80;
        server_name     css.live.tv.itc.cn;
        proxy_redirect          off;
        proxy_set_header        Host css.live.tv.itc.cn;
        proxy_set_header        X-Real-IP $remote_addr;
        proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout   60;
        proxy_send_timeout      60;
        proxy_read_timeout      60;
        proxy_buffering         off;
        proxy_buffer_size       8k;
        proxy_buffers           8 32k;
        proxy_busy_buffers_size 64k;
        proxy_headers_hash_bucket_size  64;
        proxy_headers_hash_max_size     512;
        proxy_ignore_client_abort       on;
        proxy_temp_file_write_size      64k;
        proxy_max_temp_file_size        0;
        location  / {
            root /home/liulyliu/myproxy/zebra;
            if (!-f $request_filename) {
                proxy_pass      http://192.168.20.185;
                #proxy_pass      http://10.11.5.95;
                #proxy_pass      http://tv.sohu.com/$uri;
            }
            if (-f $request_filename) {
            }
        }
    }

    #ux
   server {
        listen          80;
        server_name     www.uiueux.com;
        proxy_redirect          off;
        proxy_set_header        Host www.uiueux.com;
        proxy_set_header        X-Real-IP $remote_addr;
        proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout   60;
        proxy_send_timeout      60;
        proxy_read_timeout      60;
        proxy_buffering         off;
        proxy_buffer_size       8k;
        proxy_buffers           8 32k;
        proxy_busy_buffers_size 64k;
        proxy_headers_hash_bucket_size  64;
        proxy_headers_hash_max_size     512;
        proxy_ignore_client_abort       on;
        proxy_temp_file_write_size      64k;
        proxy_max_temp_file_size        0;

        location  / {
            root /home/liulyliu/test/ux;
            if (!-f $request_filename) {
                proxy_pass      http://64.90.48.37;
            }
            if (-f $request_filename) {
            }
        }
    }
   server {
        listen          80;
        server_name     www.zooooooom.com;
        proxy_redirect          off;
        proxy_set_header        Host www.zooooooom.com;
        proxy_set_header        X-Real-IP $remote_addr;
        proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout   60;
        proxy_send_timeout      60;
        proxy_read_timeout      60;
        proxy_buffering         off;
        proxy_buffer_size       8k;
        proxy_buffers           8 32k;
        proxy_busy_buffers_size 64k;
        proxy_headers_hash_bucket_size  64;
        proxy_headers_hash_max_size     512;
        proxy_ignore_client_abort       on;
        proxy_temp_file_write_size      64k;
        proxy_max_temp_file_size        0;

        location  / {
            root /home/liulyliu/test/ux;
            if (!-f $request_filename) {
                proxy_pass      http://64.90.48.239;
            }
            if (-f $request_filename) {
            }
        }
    }
    #js
    server {
        listen          80;
        server_name    js.live.tv.itc.cn;
        proxy_redirect          off;
        proxy_set_header        Host js.live.tv.itc.cn;
        proxy_set_header        X-Real-IP $remote_addr;
        proxy_set_header        X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_connect_timeout   60;
        proxy_send_timeout      60;
        proxy_read_timeout      60;
        proxy_buffering         off;
        proxy_buffer_size       8k;
        proxy_buffers           8 32k;
        proxy_busy_buffers_size 64k;
        proxy_headers_hash_bucket_size  64;
        proxy_headers_hash_max_size     512;
        proxy_ignore_client_abort       on;
        proxy_temp_file_write_size      64k;
        proxy_max_temp_file_size        0;

        location  / {
            root /home/liulyliu/myproxy/zebra;
            if (!-f $request_filename) {
                proxy_pass      http://192.168.20.185;
                #proxy_pass      http://10.11.5.95;
                #proxy_pass      http://tv.sohu.com/$uri;
            }
            if (-f $request_filename) {
            }
        }
    }
}
